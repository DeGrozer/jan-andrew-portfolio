export default function Loading() {
    return (
        <main className="container-editorial py-10" aria-busy="true" aria-live="polite">
            <p className="label-mono">Loading portfolio...</p>
        </main>
    );
}
